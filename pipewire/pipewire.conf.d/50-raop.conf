# Discover AirPlay/RAOP receivers such as HomePod on the local network and
# expose them to PipeWire applications as selectable audio outputs.
context.modules = [
    {
        name = libpipewire-module-raop-discover
        flags = [ ifexists nofail ]
        condition = [ { module.raop = !false } ]
    }
]
